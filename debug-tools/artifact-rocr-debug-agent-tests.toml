# rocr-debug-agent-tests
[components.doc."debug-tools/rocr-debug-agent-tests/stage"]
[components.test."debug-tools/rocr-debug-agent-tests/stage"]
include = [
  "bin/**",
  "lib/**",
  "src/**",
  # New location of the tests. The old artifacts are kept for backwards
  # compatibility.
  "tests/**"
]
